spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/freemix
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    mongodb:
      host: localhost
      authentication-database: admin
      port: 27017
      database: freemix
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: Ov23liOK5XFcdhjSuZDB
            clientSecret: 510b7bb65f0e01ccf5c09271cf96f49a9ef81aa7
            scope: read:user,user:email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user
            user-name-attribute: login

  mail:
    host: smtp.qq.com
    port: 465
    username: ${MAIL_USERNAME:3687649273@qq.com}
    password: ${MAIL_PASSWORD:qevablklknpbdada}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory

justauth:
  type:
    qq:
      client-id: ${QQ_CLIENT_ID:1903677422}
      client-secret: ${QQ_CLIENT_SECRET:eDXcU7WgdMq55qMe}
      redirect-uri: ${QQ_REDIRECT_URI:http://localhost:8888/auth/qq/callback}

server:
  port: 8888

logging:
  level:
    org:
      springframework:
        messaging: DEBUG
        web:
          socket: DEBUG

deepseek:
  base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
  api-key: ${DEEPSEEK_API_KEY:}
  model: ${DEEPSEEK_MODEL:deepseek-v4-pro}
